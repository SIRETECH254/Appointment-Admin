import PagePlaceholder from '../../../components/layout/PagePlaceholder'

const RoleList = () => (
  <PagePlaceholder title="Roles" description="Manage role permissions." />
)

export default RoleList
