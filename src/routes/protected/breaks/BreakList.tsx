import PagePlaceholder from '../../../components/layout/PagePlaceholder'

const BreakList = () => (
  <PagePlaceholder title="Breaks" description="Manage staff breaks and schedules." />
)

export default BreakList
